// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/deicod/tarife/ent/plan"
	"github.com/deicod/tarife/ent/promo"
	"github.com/rs/xid"
)

// Promo is the model entity for the Promo schema.
type Promo struct {
	config `json:"-"`
	// ID of the ent.
	ID xid.ID `json:"id,omitempty"`
	// Description holds the value of the "description" field.
	Description string `json:"description,omitempty"`
	// DiscountCents holds the value of the "discount_cents" field.
	DiscountCents int `json:"discount_cents,omitempty"`
	// MonthsApplies holds the value of the "months_applies" field.
	MonthsApplies int `json:"months_applies,omitempty"`
	// StartsAt holds the value of the "starts_at" field.
	StartsAt time.Time `json:"starts_at,omitempty"`
	// EndsAt holds the value of the "ends_at" field.
	EndsAt time.Time `json:"ends_at,omitempty"`
	// Conditions holds the value of the "conditions" field.
	Conditions string `json:"conditions,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the PromoQuery when eager-loading is set.
	Edges        PromoEdges `json:"edges"`
	plan_promos  *xid.ID
	selectValues sql.SelectValues
}

// PromoEdges holds the relations/edges for other nodes in the graph.
type PromoEdges struct {
	// Plan holds the value of the plan edge.
	Plan *Plan `json:"plan,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
	// totalCount holds the count of the edges above.
	totalCount [1]map[string]int
}

// PlanOrErr returns the Plan value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e PromoEdges) PlanOrErr() (*Plan, error) {
	if e.Plan != nil {
		return e.Plan, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: plan.Label}
	}
	return nil, &NotLoadedError{edge: "plan"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Promo) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case promo.FieldDiscountCents, promo.FieldMonthsApplies:
			values[i] = new(sql.NullInt64)
		case promo.FieldDescription, promo.FieldConditions:
			values[i] = new(sql.NullString)
		case promo.FieldStartsAt, promo.FieldEndsAt:
			values[i] = new(sql.NullTime)
		case promo.FieldID:
			values[i] = new(xid.ID)
		case promo.ForeignKeys[0]: // plan_promos
			values[i] = &sql.NullScanner{S: new(xid.ID)}
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Promo fields.
func (_m *Promo) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case promo.FieldID:
			if value, ok := values[i].(*xid.ID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case promo.FieldDescription:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field description", values[i])
			} else if value.Valid {
				_m.Description = value.String
			}
		case promo.FieldDiscountCents:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field discount_cents", values[i])
			} else if value.Valid {
				_m.DiscountCents = int(value.Int64)
			}
		case promo.FieldMonthsApplies:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field months_applies", values[i])
			} else if value.Valid {
				_m.MonthsApplies = int(value.Int64)
			}
		case promo.FieldStartsAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field starts_at", values[i])
			} else if value.Valid {
				_m.StartsAt = value.Time
			}
		case promo.FieldEndsAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field ends_at", values[i])
			} else if value.Valid {
				_m.EndsAt = value.Time
			}
		case promo.FieldConditions:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field conditions", values[i])
			} else if value.Valid {
				_m.Conditions = value.String
			}
		case promo.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field plan_promos", values[i])
			} else if value.Valid {
				_m.plan_promos = new(xid.ID)
				*_m.plan_promos = *value.S.(*xid.ID)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Promo.
// This includes values selected through modifiers, order, etc.
func (_m *Promo) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryPlan queries the "plan" edge of the Promo entity.
func (_m *Promo) QueryPlan() *PlanQuery {
	return NewPromoClient(_m.config).QueryPlan(_m)
}

// Update returns a builder for updating this Promo.
// Note that you need to call Promo.Unwrap() before calling this method if this Promo
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Promo) Update() *PromoUpdateOne {
	return NewPromoClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Promo entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Promo) Unwrap() *Promo {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Promo is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Promo) String() string {
	var builder strings.Builder
	builder.WriteString("Promo(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("description=")
	builder.WriteString(_m.Description)
	builder.WriteString(", ")
	builder.WriteString("discount_cents=")
	builder.WriteString(fmt.Sprintf("%v", _m.DiscountCents))
	builder.WriteString(", ")
	builder.WriteString("months_applies=")
	builder.WriteString(fmt.Sprintf("%v", _m.MonthsApplies))
	builder.WriteString(", ")
	builder.WriteString("starts_at=")
	builder.WriteString(_m.StartsAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("ends_at=")
	builder.WriteString(_m.EndsAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("conditions=")
	builder.WriteString(_m.Conditions)
	builder.WriteByte(')')
	return builder.String()
}

// Promos is a parsable slice of Promo.
type Promos []*Promo
