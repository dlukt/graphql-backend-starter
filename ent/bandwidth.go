// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/deicod/tarife/ent/bandwidth"
	"github.com/deicod/tarife/ent/plan"
	"github.com/rs/xid"
)

// Bandwidth is the model entity for the Bandwidth schema.
type Bandwidth struct {
	config `json:"-"`
	// ID of the ent.
	ID xid.ID `json:"id,omitempty"`
	// DownMbps holds the value of the "down_mbps" field.
	DownMbps int `json:"down_mbps,omitempty"`
	// UpMbps holds the value of the "up_mbps" field.
	UpMbps int `json:"up_mbps,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the BandwidthQuery when eager-loading is set.
	Edges          BandwidthEdges `json:"edges"`
	plan_bandwidth *xid.ID
	selectValues   sql.SelectValues
}

// BandwidthEdges holds the relations/edges for other nodes in the graph.
type BandwidthEdges struct {
	// Plan holds the value of the plan edge.
	Plan *Plan `json:"plan,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
	// totalCount holds the count of the edges above.
	totalCount [1]map[string]int
}

// PlanOrErr returns the Plan value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e BandwidthEdges) PlanOrErr() (*Plan, error) {
	if e.Plan != nil {
		return e.Plan, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: plan.Label}
	}
	return nil, &NotLoadedError{edge: "plan"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Bandwidth) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case bandwidth.FieldDownMbps, bandwidth.FieldUpMbps:
			values[i] = new(sql.NullInt64)
		case bandwidth.FieldID:
			values[i] = new(xid.ID)
		case bandwidth.ForeignKeys[0]: // plan_bandwidth
			values[i] = &sql.NullScanner{S: new(xid.ID)}
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Bandwidth fields.
func (_m *Bandwidth) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case bandwidth.FieldID:
			if value, ok := values[i].(*xid.ID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case bandwidth.FieldDownMbps:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field down_mbps", values[i])
			} else if value.Valid {
				_m.DownMbps = int(value.Int64)
			}
		case bandwidth.FieldUpMbps:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field up_mbps", values[i])
			} else if value.Valid {
				_m.UpMbps = int(value.Int64)
			}
		case bandwidth.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field plan_bandwidth", values[i])
			} else if value.Valid {
				_m.plan_bandwidth = new(xid.ID)
				*_m.plan_bandwidth = *value.S.(*xid.ID)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Bandwidth.
// This includes values selected through modifiers, order, etc.
func (_m *Bandwidth) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryPlan queries the "plan" edge of the Bandwidth entity.
func (_m *Bandwidth) QueryPlan() *PlanQuery {
	return NewBandwidthClient(_m.config).QueryPlan(_m)
}

// Update returns a builder for updating this Bandwidth.
// Note that you need to call Bandwidth.Unwrap() before calling this method if this Bandwidth
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Bandwidth) Update() *BandwidthUpdateOne {
	return NewBandwidthClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Bandwidth entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Bandwidth) Unwrap() *Bandwidth {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Bandwidth is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Bandwidth) String() string {
	var builder strings.Builder
	builder.WriteString("Bandwidth(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("down_mbps=")
	builder.WriteString(fmt.Sprintf("%v", _m.DownMbps))
	builder.WriteString(", ")
	builder.WriteString("up_mbps=")
	builder.WriteString(fmt.Sprintf("%v", _m.UpMbps))
	builder.WriteByte(')')
	return builder.String()
}

// Bandwidths is a parsable slice of Bandwidth.
type Bandwidths []*Bandwidth
